@model Smart.Models.Cliente
@{
    
    ViewData["Title"]= "Detalhes";

}


<div class="container">
    <h2 class="my-4">Detalhes</h2>
 
    <div class="card">
        <div class="card-header">
            Detalhes do Cliente:
        </div>
        <div class="card-body">
            <div class="row">
              
                    <div class="col-md-6">
                        <h4>Nome: @Html.DisplayFor(model => model.Nome)</h4>
                        <ul class="list-group list-group-flush">
                            <li class="list-group-item">ID: @Html.DisplayFor(model => model.Id)</li>
                            <li class="list-group-item">Nome: @Html.DisplayFor(model => model.Nome)</li>
                            <li class="list-group-item">Email: @Html.DisplayFor(model => model.Contatos.Email)</li>
                             <li class="list-group-item">Telefone: @Html.DisplayFor(model => model.Contatos.Telefone)</li>
                            <li class="list-group-item">Endereço: @Html.DisplayFor(model => model.Contatos.Endereco)</li>
                        </ul>
                    </div>
               
            </div>
        </div>
    </div>
 <div class="mt-4">
        <form asp-action="Delete">
        <input type="hidden" asp-for="Id"/>
        <input type="submit" class="btn btn-danger" value="Deletar">
        <a asp-action="Index" class="btn btn-primary">Voltar</a>
        <a asp-action="Edit" class="btn btn-warning" asp-route-id="@Model.Id">Edit</a>
    </form>
</div>
   
</div>